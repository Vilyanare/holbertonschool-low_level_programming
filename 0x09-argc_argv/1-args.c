#include "holberton.h"
#include <stdio.h>
/**
 *main - prints how many arguments in program
 *@argc: argument count
 *@argv: argument array
 *Return: always 0
 */
int main(int argc, char **argv)
{
	(void)argv;
	printf("%d\n", argc);
	return (0);
}
