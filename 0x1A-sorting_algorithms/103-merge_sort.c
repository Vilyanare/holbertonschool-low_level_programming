#include "sort.h"
void split()
{

}
void merge()
{

}
void merge_sort(int *array, size_t size)
{
	int *new = NULL;
	size_t i = 0;

	new = malloc(sizeof(int) * size);
	if (new == NULL || array == NULL || size < 2)
		return;
	for (i = 0; i < size; i++)
	{

	}
}
